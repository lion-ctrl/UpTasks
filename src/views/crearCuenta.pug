<!DOCTYPE html>
html(lang="en")
    head
        include ./partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            if locals.mensajes
                //- pre= var_dump(mensajes)
                //- pre= var_dump(locals.mensajes)
                //- each categoria in Object.keys(locals.mensajes)
                //-     pre= var_dump(categoria)
                //- Al final son lo mismo
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in locals.mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form(action="/crear-cuenta" method="POST")#formulario.caja.caja-login
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Contraseña" value= password)
                .campo.enviar
                    input(type="submit" value="Crear Cuenta").boton
                .campo
                    a(href="/iniciar-sesion") Volver a Iniciar Sesión