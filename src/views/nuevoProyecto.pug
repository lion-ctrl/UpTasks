extends ./layout/layout
include ./mixins/crearProyecto

block contenido
    .contenido-principal
        h1 #{nombrePagina}
        if errores
            each error in errores
                .alerta.error=error.texto
        +crearProyecto(proyecto)
        // mixin en pug funciones